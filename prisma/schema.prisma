// This is your Prisma schema file,
// Learn more about it in the docs: https://prisma.link/d/prisma-schema

// Looking for ways to speed up your queries, on scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Author {
  id          String   @id @default(cuid())
  name        String
  email       String   @unique
  bio         String?
  nationality String?
  birthYear   Int?
  books       Book[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Book {
  id            String   @id @default(cuid())
  title         String
  description   String?
  isbn          String?  @unique
  publishedYear Int?
  genre         String?
  pages         Int?
  authorId      String
  author        Author   @relation(fields: [authorId], references: [id], onDelete: Cascade)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([authorId])
  @@index([genre])
  @@index([title])
}
